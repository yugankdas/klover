# Klover

Klover is a lightweight DSL that compiles `.kv` files into styled HTML.

## Getting Started

1. Create an `input.kv` file.
2. Run the compiler:
   ```bash
   npm start
   ```
3. Open `output.html` to see the result.

## Documentation
For more details on the project architecture and roadmap, see [PROJECT_CONTEXT.md](./PROJECT_CONTEXT.md).
